目标：
会用加减乘除、取模
知道整数除法和小数除法的区别
会用比较和逻辑运算，理解短路
知道运算优先级，必要时加括号
会用复合赋值（+=、-= 等）

要点小抄：
算术：+ - * / %（% 只用于整数）
整数除法会截断小数；想要小数，用 3.0 或 (double)a
比较：== != < > <= >=（结果是 0 或 1）
逻辑：&&（且）、||（或）、!（非），有短路
复合赋值：x += 3 等价 x = x + 3
优先级：* / % 高于 + -；用括号最清晰
不要在同一表达式里又使用又自增同一变量（如 i = i++ + 1）
